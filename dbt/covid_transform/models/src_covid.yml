version: 2

sources:
  - name: jhu
    database: covid
    schema: public              # schema where raw_covid_data lives
    tables:
      - name: raw_covid_data     # the table you loaded via Python
        description: "Daily COVID-19 raw data"
